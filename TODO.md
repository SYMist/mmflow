# Money Flow Planner - TODO

## Bug / 정확성

- [ ] `totalIn` 계산이 `income-salary` ID에 300만원 하드코딩 — 모든 수입 노드의 `amount` 합산으로 수정
- [ ] 노드 ID 생성이 `nodes.length + 1` 기반 — 삭제 후 재추가 시 ID 충돌 가능 (uuid 등으로 교체)
- [ ] `FlowEdge` 컴포넌트 미사용 — 삭제하거나 용도 정리

## 성능

- [ ] `App.tsx`에 모든 상태 집중 — 노드 하나 수정해도 전체 리렌더. 상태 관리 라이브러리(Zustand/Jotai) 도입 검토
- [ ] `FlowCanvas` useEffect에서 매번 전체 노드 배열 재생성 — 변경된 노드만 업데이트하도록 최적화
- [ ] `onAddNodeAround` 내부에서 `setEdges`를 `setNodes` 콜백 안에서 호출 — 배치 업데이트 보장

## UX

- [ ] 엣지 금액 편집: 선택 → 클릭 2단계 필요 — 더블클릭으로 바로 편집 모드 진입
- [ ] 노드 추가 후 바로 이름 편집 모드로 진입
- [ ] DetailPanel 입력 후 "확인" 버튼 필요 — 실시간 반영(onChange)으로 전환 검토
- [ ] Undo/Redo 미지원 — 실수로 삭제 시 복구 불가
- [ ] 키보드 단축키 (Delete로 삭제, Ctrl+Z 되돌리기 등)
- [ ] 시나리오 복수 관리 — 현재 하나만 저장 가능

## 디자인

- [ ] 노드 색상 고정 — 사용자가 색상 선택할 수 있도록
- [ ] 수입/목적지 노드 시각적 구분 부족 (아이콘, 모양 등)
- [ ] 엣지에 비율(%)만 표시 — 원화 금액도 함께 표시
- [ ] 다크모드 미지원
- [ ] 빈 상태(노드 0개) 온보딩/가이드 없음

## 기능 확장

- [ ] 다중 시나리오 저장/비교
- [ ] 월별 데이터 분리 관리 (현재 단일 상태)
- [ ] JSON export/import (백업/복원)
- [ ] 비정기 수입/지출 (보너스, 연납 보험 등) 모델링
- [ ] 목적지별 목표 금액 대비 달성률 시각화
